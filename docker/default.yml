services:
  postgres:
    image: library/postgres:18-alpine
    container_name: greedybear_postgres
    restart: unless-stopped
    volumes:
      - postgres_data:/var/lib/postgresql
    env_file:
      - ./env_file_postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]  
      interval: 10s
      timeout: 2s
      retries: 3
      start_period: 10s
      start_interval: 1s

  app:
    image: intelowlproject/greedybear:prod
    container_name: greedybear_app
    restart: unless-stopped
    stop_grace_period: 30s
    volumes:
      - generic_logs:/var/log/greedybear
      - static_content:/opt/deploy/greedybear/static
    entrypoint:
      - ./docker/entrypoint_gunicorn.sh
    command: sh -c "gunicorn greedybear.wsgi:application --bind 0.0.0.0:8001 --protocol uwsgi --workers $$((2 * $$(nproc) + 1)) --graceful-timeout 30 --timeout 120"
    expose:
      - "8001"
    env_file:
      - env_file
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import socket; socket.create_connection(('localhost', 8001), timeout=2)\" || exit 1"]
      interval: 10s
      timeout: 2s
      retries: 30
      start_period: 120s
      start_interval: 1s

  nginx:
    image: intelowlproject/greedybear_nginx:prod
    container_name: greedybear_nginx
    restart: unless-stopped
    volumes:
      - ../configuration/nginx/http.conf:/etc/nginx/conf.d/default.conf
      - ../configuration/nginx/errors.conf:/etc/nginx/errors.conf
      - ../configuration/nginx/locations.conf:/etc/nginx/locations.conf
      - nginx_logs:/var/log/nginx
      - static_content:/var/www/static
    ports:
      - "80:80"
    depends_on:
      app:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost/hc || exit 1"]
      interval: 10s
      timeout: 2s
      retries: 3
      start_period: 10s
      start_interval: 1s


  qcluster:
    image: intelowlproject/greedybear:prod
    container_name: greedybear_qcluster
    restart: unless-stopped
    stop_grace_period: 3m
    command: sh -c "python manage.py setup_schedules && exec python manage.py qcluster"
    volumes:
      - generic_logs:/var/log/greedybear
      - mlmodels:/opt/deploy/greedybear/mlmodels
    env_file:
      - env_file
    depends_on:
      postgres:
        condition: service_healthy
      app:
        condition: service_healthy
    user: "2000:82"
    healthcheck:
      disable: true

volumes:
  postgres_data:
  nginx_logs:
  generic_logs:
  static_content:
  mlmodels: